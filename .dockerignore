# Keep the build context to what the image is built from. The UI's markdown
# libraries are fetched inside the build at their pinned versions, so a copy
# left in a working tree never decides what ships.
.git
server/web/assets/
cli/dist/
